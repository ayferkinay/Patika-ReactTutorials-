const topla = (a,b)=> a+b;
const cikar = (a,b)=> a-b;

const text  = ("text")

const hello = (name) =>  {
    console.log(`Hello ${name}`);
}

const product = {
    productName : "kalem",
    productId : "2"
}

const  products = [
    {
        productName : "kitap",
        productId : "2"
    },
    {
        productName : "kağıt",
        productId : "2"
    }
]

export {topla, cikar, hello, text, product, products} ;